interface FriendInterface {
    friendsNumber: number;
    friends: [
        avatar: string,
        name: string,
        status: string,
        email: string,
        rate: number,
    ]
}

export default FriendInterface